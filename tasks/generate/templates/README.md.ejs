# <%= package.fullName %>

> <%= description %>

## Install

```bash
yarn add <%= package.fullName %>
```

## Usage

```js
// minified
import <%= package.pascalCaseName %> from '<%= package.fullName %>'

// not minified
import <%= package.pascalCaseName %> from '<%= package.fullName %>/umd/<%= package.name %>.js'
<% if (features.polyfilled) { -%>

// polyfilled
import <%= package.pascalCaseName %> from '<%= package.fullName %>/umd/<%= package.name %>.polyfilled.js'

// minified and polyfilled
import <%= package.pascalCaseName %> from '<%= package.fullName %>/umd/<%= package.name %>.polyfilled.min.js'
<% } -%>
```

## Development

```bash
<% if (repository) { -%>
# clone repo
git clone <%= repository %>
cd <%= package.name %>

<% } -%>
# install dependencies
yarn

# Run example and go to http://localhost:1234
npm run dev
```

## Build

```bash
npm run build
```
