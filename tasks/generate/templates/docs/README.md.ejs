# <%= libraryName -%>  
  
> <%= libraryDescription -%>  
  
## Install

```bash
yarn add <%= fullPackageName -%>  
```

## Usage

```js
// minified
import <%= packageNamePascalCase -%> from '<%= fullPackageName -%>'

// not minified
import <%= packageNamePascalCase -%> from '<%= fullPackageName -%>/umd/<%= packageName -%>.js'
<% if (features.polyfilled) { -%>

// polyfilled
import <%= packageNamePascalCase -%> from '<%= fullPackageName -%>/umd/<%= packageName -%>.polyfilled.js'

// minified and polyfilled
import <%= packageNamePascalCase -%> from '<%= fullPackageName -%>/umd/<%= packageName -%>.polyfilled.min.js'
<% } -%>

```

## Development

```bash
<% if (repositoryUrl.length > 0) { -%>
# clone repo
git clone <%= repositoryUrl -%>
cd <%= packageName -%>

<% } -%>
# install dependencies
yarn

# Run example and go to http://localhost:1234
npm run dev
```

## Build

```bash
npm run build
```
